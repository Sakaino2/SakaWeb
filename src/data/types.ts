interface ExperienceData {
    title: string,
    developer: ExperienceType,
    graphicDesigner: ExperienceType
}

interface ExperienceType {
    title: string,
    img:string,
    description: string,
}